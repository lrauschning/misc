/*package sort;

import java.util.List;
import java.util.ArrayList;
import java.util.Comparator;
import java.util.function.Function;

@Sorter.LinearSorter
public class CountingSorter implements Sorter{
	public <T> List<T> sort(List<T> list, Comparator<? super T> comp, int start, int end){
		if(T != Integer)
			throw new RuntimeException("only Integers can be sorted with CountingSort!");
		Integer[] keys = new Integer[cap]; // for each index, store the associated Integer value; maybe use hashmap?
		int[] occs = new int[cap];

	}
}
*/
